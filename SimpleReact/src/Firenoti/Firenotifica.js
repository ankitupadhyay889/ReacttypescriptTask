import React from 'react';
import { requestForToken } from './Fire';

const Firenotifica = () => {

    requestForToken();

    return (
        <div>
            
        </div>
    );
};

export default Firenotifica;